version: '3.8'

services:
  atelier-web:
    build:
      context: ./
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
    command: npm run dev
    ports:
      - '3001:3001'
    environment:
      NODE_ENV: development
